import 'package:flutter/material.dart';

class DonhangSreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Đơn hàng'),
      ),
      body: Center(
        child: Text('Đây là màn hình đơn hàng'),
      ),
    );
  }
}
